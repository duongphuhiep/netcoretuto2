<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>12-Degree Diagonal Splitter</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            height: 100vh;
            overflow: hidden;
        }

        .container {
            height: 100vh;
            position: relative;
            display: flex;
            flex-direction: column;
        }

        .top-section {
            background-color: #808080;
            height: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
            position: relative;
            z-index: 1;
        }

        .bottom-section {
            background-color: #ff8c00;
            height: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            font-weight: bold;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.3);
            position: relative;
            z-index: 1;
        }

        .splitter {
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 200px;

            z-index: 2;
        }

        .splitter::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 100%;
            background: linear-gradient(12deg, #ff8c00 50%, #808080 50%);
        }

        /* Alternative method using clip-path for more precise control */
        .splitter-clippath {
            position: absolute;
            top: 50%;
            left: 0;
            right: 0;
            height: 0;
            transform: translateY(-50%);
            z-index: 2;
            border-top: 50px solid #808080;
            border-bottom: 50px solid #ff8c00;
            transform: translateY(-50%) skewX(-12deg);
            transform-origin: center;
        }

        /* Demo content styling */
        .content {
            padding: 20px;
        }

        .switch-button {
            position: fixed;
            top: 20px;
            right: 20px;
            background: rgba(0,0,0,0.7);
            color: white;
            border: none;
            padding: 10px 15px;
            border-radius: 5px;
            cursor: pointer;
            z-index: 10;
            font-size: 12px;
        }

        .switch-button:hover {
            background: rgba(0,0,0,0.9);
        }

        /* Hide one method by default */
        .method-2 {
            display: flex;
        }
    </style>
</head>
<body>

    <div class="container method-1">
        <!-- <div class="top-section">
            <div class="content">Gray Section (Above)</div>
        </div> -->
        <div class="splitter"></div>
        <!-- <div class="bottom-section">
            <div class="content">Orange Section (Below)</div>
        </div> -->
    </div>

<!--     <div class="container method-2 flex">
        <div class="top-section">
            <div class="content">Gray Section (Above)</div>
        </div>
        <div class="splitter-clippath"></div>
        <div class="bottom-section">
            <div class="content">Orange Section (Below)</div>
        </div>
    </div>
 -->

</body>
</html>